fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
pyyaml>=6.0,<7.0

# ML / retrieval stack
sentence-transformers>=2.6,<4.0
transformers>=4.39,<5.0
qdrant-client>=1.8,<2.0
rank-bm25>=0.2.2,<0.3
rapidfuzz>=3.6,<4.0
# Russian lemmatization. pymorphy2 (0.9.1) installs cleanly on Windows/Python
# 3.11 but is broken at runtime here: it calls inspect.getargspec, which was
# removed in Python 3.10+ ("AttributeError: module 'inspect' has no attribute
# 'getargspec'"). Confirmed by direct test in this environment, so we use the
# actively maintained fork pymorphy3 instead. The choice is isolated entirely
# inside app/lexical/tokenizer.py (try pymorphy2, fall back to pymorphy3) so
# nothing else in the codebase references either package directly.
pymorphy3>=1.2,<2.0
pymorphy3-dicts-ru

structlog>=24.1,<25.0
numpy>=1.26,<3.0

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<0.24
pytest-cov>=5.0,<7.0
httpx>=0.27,<1.0
# Load testing (tests/load/locustfile.py). Only needed to run the load
# scenarios manually against a running server; not imported by pytest.
locust>=2.20,<3.0

# File format support (folder indexing)
python-docx>=1.0,<2.0
striprtf>=0.0.30,<1.0
charset-normalizer>=3.0,<4.0
