fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
pyyaml>=6.0,<7.0

# ML / retrieval stack (heavy, installed starting Phase 2)
sentence-transformers>=2.6,<4.0
transformers>=4.39,<5.0
qdrant-client>=1.8,<2.0
rank-bm25>=0.2.2,<0.3
rapidfuzz>=3.6,<4.0
# pymorphy2 for Russian lemmatization; if the C-extension (DAWG) fails to
# build on Windows/Python 3.11+, fall back to pymorphy3 (isolated behind
# app/lexical/tokenizer.py so the swap needs no changes elsewhere).
pymorphy2>=0.9.1,<0.10

structlog>=24.1,<25.0
numpy>=1.26,<3.0

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<0.24
httpx>=0.27,<1.0
